# 패치 노트

무능한 날씨예측기의 변경 이력입니다. 기능이 추가되거나 바뀔 때마다 여기에 기록합니다.

## v1.0.55 (2026-08-26)

1.0.54 에서 새로 손댄 부분을 한 번 더 뜯어보고 남은 결함을 정리했습니다.

### 수정
- 순서를 빠르게 두 번 바꾸면 카드 하나가 다른 카드 위에 겹친 채 굳던 문제
- 순서를 한 번이라도 옮긴 카드만 날씨를 갱신할 때 깜빡이던 문제
- 순서 변경 중에 광고를 누르면 새 창이 열리면서 하던 정렬이 날아가던 문제
- 알림 구독이 자동으로 갱신될 때 저장에 실패하면, 앱은 켜짐으로 보이는데 알림은 오지 않던 문제
- 알림을 켜는 중 연결이 끊기면 저장됐는지 모르는 채로 시간을 되돌려, 실제 저장된 값과 화면이 반대가 되던 문제
- 알림 처리가 끝나기 전에 설정 창이 닫혀 결과 안내를 못 보던 문제
- 브라우저 쪽 구독만 사라진 경우 알림을 꺼도 서버에 기록이 남던 문제
- 갱신 실패 버튼이 너무 작아 누르기 어렵던 문제, 화면 낭독기에 실패가 전달되지 않던 문제
- 오류 화면의 보조 버튼이 주 버튼과 똑같이 생겨 어느 쪽이 기본인지 알 수 없던 문제

## v1.0.54 (2026-08-26)

한 번 더 전체를 훑어 결함을 잡고, 첫 화면이 빨리 뜨도록 손봤습니다.

### 수정
- 알림 시간 변경이 항상 실패하던 문제: 저장 요청이 잘못된 기준으로 중복 판정돼 거절되고 있었습니다. 저장과 해제를 전용 함수로 바꿨습니다
- 알림을 끄지 못했는데도 꺼진 것처럼 표시되던 문제: 앱을 켜자마자 끄면 아무 일도 일어나지 않은 채 성공으로 보였습니다
- 알림 실패 안내가 원인과 상관없이 늘 "권한이 필요해요"로 나오던 문제: 권한, 준비 미완료, 저장 실패를 구분해 알려줍니다
- 브라우저에서 알림을 차단해 둔 경우 아무 반응이 없던 문제: 차단 상태를 알려주고 푸는 방법을 안내합니다
- 알림 관련 안내가 설정을 닫은 한참 뒤에 팝업으로 뜨던 문제: 설정 패널 안에 문구로 남깁니다
- 갱신에 실패했을 때 지역 이름은 새 지역, 날씨는 이전 지역인 화면이 그대로 보이던 문제: 실패하면 이전 지역 그대로 유지합니다
- 위치를 찾는 동안 다른 지역을 고르면 잠시 뒤 원래대로 튕기던 문제
- 오류 화면의 "현재 위치로 보기"가 눌러도 아무 일도 없던 문제, 그리고 기본 지역 설정까지 말없이 바꾸던 문제
- 갱신 중 화면이 흐려지는 효과가 지역 전환 애니메이션에 눌려 반대 순서로 보이던 문제
- 아이패드에서 알림 안내가 나오지 않던 문제, 알림이 켜진 상태에서도 끌 수 없던 경우
- 푸시 구독이 갱신되면 알림이 조용히 끊기던 문제: 서비스워커가 스스로 다시 등록합니다
- 알림 구독 정보에 아무나 접근할 수 있던 문제: 저장과 해제 두 가지만 열어두고 나머지는 막았습니다

### 변경
- 첫 화면 로딩 용량을 줄였습니다: 지도와 레이더 해석기는 레이더를 볼 때 불러오고, 리액트는 따로 떼어 다음 방문에 다시 받지 않습니다
- 배경 날씨 애니메이션이 고해상도 화면에서 또렷해졌습니다. 맑음·구름·안개처럼 거의 멈춰 있는 연출은 프레임을 줄여 배터리를 덜 씁니다
- 설정 창을 Esc 키나 바깥쪽 클릭으로 닫을 수 있고, 열면 창으로 포커스가 이동합니다
- 최하단에 무엇을 저장하고 무엇을 저장하지 않는지 적었습니다

## v1.0.53 (2026-08-25)

코드 전반을 점검해 실제로 겪을 수 있는 결함을 정리했습니다.

### 수정
- 지역을 빠르게 바꿀 때 먼저 요청한 지역 날씨가 나중에 도착해 엉뚱한 지역이 표시되던 문제
- 지역을 바꿀 때마다 화면 전체가 로딩으로 바뀌던 동작 개선: 기존 화면을 유지하고 헤더에 "날씨 갱신 중"만 표시
- 시간대별 카드가 기기 시간을 기준으로 그려져, 다른 시간대 지역에서 "지금" 위치가 어긋나던 문제
- 지역 비교의 주간 표가 날짜가 아니라 순서로 짝지어지던 문제
- 정렬 모드에서 카드나 칩을 탭하면 아래의 동작(지역 변경 등)이 그대로 실행되던 문제
- 순서 변경 애니메이션이 진행 중인 위치를 측정해 카드가 반대로 튀던 문제
- 화면이 꺼져 있는 동안 순서를 바꾸면 카드가 어긋난 자리에 굳던 문제
- 즐겨찾기 칩 정렬 모드에서 페이지 세로 스크롤이 막히던 문제
- 레이더가 한 번 실패하면 지역을 바꿔도 실패 문구가 남던 문제, 전환 중 이전 지역 타임라인이 남던 문제
- 알림 시간 변경이 서버에 저장되지 못해도 성공한 것처럼 보이던 문제: 실패하면 원래 시간으로 되돌리고 알려줍니다
- 아이폰에서 알림이 "지원되지 않아요"로만 안내되던 문제: 홈 화면에 추가하면 쓸 수 있다고 안내합니다
- 저장된 즐겨찾기나 화면 순서가 손상됐을 때 같은 카드가 두 번 그려지거나 정렬이 먹지 않던 문제
- 날씨를 불러오지 못한 화면에서 빠져나올 수 없던 경우: "현재 위치로 보기" 추가
- 쿠팡 배너가 폭 측정에 실패하면 끝내 표시되지 않던 문제
- 오프라인 캐시가 실패 응답까지 저장해 낡은 화면이 남던 문제 (캐시를 새로 만듭니다)

## v1.0.52 (2026-08-23)

### 변경
- 시간대별 카드 디자인 개선: 온도 격자선·낮 시간대 배경 밴드·곡선 아래 그라데이션 채움·현재 시각 세로선·최고최저 라벨·하단 요약 문구
- 순서를 바꿀 때 카드가 새 위치로 미끄러지는 애니메이션(FLIP) 추가

## v1.0.51 (2026-08-23)

### 추가
- 시간대별 기온·강수 카드: 앞으로 24시간 기온 곡선(어제 같은 시간대는 점선으로 겹침) + 강수량 막대
- 메인 화면 카드 순서 커스터마이즈: 카드를 길게 눌러 끌어서 바꾸기(아이폰 홈 화면처럼), 설정 > 🧩 화면 순서에서도 ▲▼로 변경, 기본 순서로 되돌리기
- 즐겨찾기 칩도 길게 눌러 끌어서 순서 변경

### 변경
- 실용 멘트 기본 상태를 펼침으로
- 순서를 바꿀 수 있다는 안내 문구를 화면 하단과 설정에 표기

## v1.0.5 (2026-08-21)

### 변경
- 실용 멘트를 비주얼 카드로: 움직이는 큰 이모지 + 핵심 키워드(예: 장우산 + 방수 신발)가 먼저 보이고, 긴 설명은 "자세히 보기"로 펼침
- 즐겨찾기·지역을 바꿀 때 화면이 부드럽게 전환되는 애니메이션
- 상단에 "지금 · 현재 날씨" 표기를 크게, 배경은 테마색 혼합을 줄여 맑음/흐림/비/눈 등 현재 날씨별 색 차이가 분명하게

### 수정
- 폭우(50mm↑)인 날 "애매한 하늘, 접이식 우산" 멘트가 함께 뜨던 모순 수정
- 비 오는 날 "반팔 날씨, 활동하기 좋습니다" 멘트가 어색하던 문구 수정

## v1.0.4 (2026-08-21)

### 변경
- 상단 현재 정보를 아이콘 칩으로 재구성: 체감 / 습도 / 강수확률 / 오늘 강수(비 올 때는 시간당)
- "오늘 vs 어제" 카드를 "어제와 비교하면"으로 바꾸고, 기온·강수·바람을 한 문장으로 요약해 상단에 표시
- 날씨 애니메이션을 카드 위로 올려 화면 전체에서 보이게 (클릭은 그대로 통과), 카드 투명도 소폭 증가, 빗줄기·눈·별 개수 증가

## v1.0.3 (2026-08-21)

### 추가
- 처음 열 때 보여줄 기본 지역 설정 (설정 > 🏠, 현재 위치 또는 즐겨찾기 중 선택)
- 즐겨찾기 순서 변경 (편집 모드에서 ◀ ▶)
- 레이더 지도에 현재 위치로 돌아가기 버튼(◎)

### 수정
- 비교 그래프 가시성 개선: 배경과 비슷해 안 보이던 막대·트랙 대비 강화 (오늘 vs 어제, 주간, 지역 비교)

## v1.0.2 (2026-08-18)

### 추가
- 쿠팡 파트너스 배너 2곳: 이번 주 날씨 아래 다이나믹 캐러셀(관심사 기반 개인화 상품), 레이더 아래 카테고리 배너

## v1.0.1 (2026-08-18)

### 추가
- 상단 메인에 현재 강수 정보 상시 노출: 강수확률, 오늘 누적 강수량, 시간당 강수량(기상청 관측, 비 올 때)

### 수정
- 좁은 화면에서 상단 날씨 텍스트가 단어 중간에서 꺾이던 줄바꿈 문제 (단어 단위 줄바꿈)
- 어제 비교 캡슐이 좁은 폰에서 아래로 떨어지던 배치 문제: 윗줄은 이모지·온도·캡슐만, 날씨 설명과 강수 정보는 아래 전체 폭으로 재배치
- 앱 푸터의 깃허브 링크 제거 (버전 숫자만 표시)
- 메인 화면의 "기상청 관측 반영" 배지 제거 (관측값은 그대로 쓰되 표시만 정리)
- 푸터 버전 표기에 블로그 패치노트 링크 연결

## v1.0.0 (2026-08-18)

첫 정식 버전. 여기까지의 모든 것.

### 핵심 기능
1. **어제와 비교**: 지금 기온이 어제 이 시간보다 몇 도 높은지 낮은지, 화살표 캡슐로 표시
2. **오늘 vs 어제 그래픽**: 기온 범위 바, 강수량, 바람(돌풍 포함)을 어제와 같은 눈금에서 비교
3. **비구름 레이더**: 기상청 실황(과거 2시간) + 초단기예측(미래 3시간)을 OSM 지도 위에서 슬라이더로 탐색
4. **현재 날씨 관측값**: 기상청 초단기실황 기반 강수형태·습도·시간당 강수량, "기상청 관측 반영" 배지
5. **아침 브리핑 알림**: 매일 06:00~09:00 중 선택한 시각에 오늘 날씨 요약 푸시
6. **내일 준비물 알림**: 21:00~23:00 중 선택한 시각에, 내일이 오늘과 크게 다를 때만 출근 준비물 안내
7. **주간 예보**: 요일별 최저~최고 온도 범위 바 (컬럼 헤더 포함)
8. **지역 비교**: 두 지역의 현재 날씨와 1주일치를 나란히 비교
9. **위치 즐겨찾기·검색**: 상시 노출 검색, 즐겨찾기 없이 바로 보기, 별표 저장
10. **오늘의 실용 멘트**: 기온 10구간·강수 6구간·바람·자외선 기반 조언 (접기 지원)

### 디자인
- 날씨 따라 움직이는 배경 (비·눈·번개·햇살·별·구름)
- 현재 날씨·밤낮에 따른 배경 그라데이션 테마 8종
- 포인트 컬러 5종 선택 (배경·카드·형광 프레임까지 반영)
- PWA 홈 화면 설치 지원

### 기타
- 위치 권한은 사용자가 직접 누를 때만 요청
- 오늘 방문자 수 표시
- 날씨 연동 쿠팡 파트너스 추천 (접속 시 1회 레이어 + 인라인 버튼)
- 오픈채팅·메일 피드백 채널, 패밀리 사이트 링크
